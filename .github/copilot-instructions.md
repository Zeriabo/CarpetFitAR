# Copilot Instructions for CarpetFitARNew

- Project bootstrapped with React Native 0.85.1 (TypeScript default)
- Migrated components, screens, and assets from legacy project
- iOS and Android supported (see README for build instructions)
- Use Xcode to set up code signing for real device deployment
- Keep this file updated with any custom project rules or migration notes
